/// The central orchestrator that drives the brain-worker pipeline.
pub struct Orchestrator {
    // TODO: registry, mcp_server, worktrees, cost_tracker, pm, config
}

impl Orchestrator {
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for Orchestrator {
    fn default() -> Self {
        Self::new()
    }
}
