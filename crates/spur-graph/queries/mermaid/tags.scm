; Flowchart vertices and named subgraphs. Sequence participants.

(flow_vertex
  (flow_vertex_id) @name) @definition.module

(flow_stmt_subgraph
  (flow_vertex_id) @name) @definition.module

(sequence_stmt_participant
  (sequence_actor) @name) @definition.module

(sequence_stmt_actor
  (sequence_actor) @name) @definition.module
