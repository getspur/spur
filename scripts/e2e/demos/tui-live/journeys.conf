# Live-project storyboard (navigation only — no agent prompts / dispatches).
# Format: name|shell_use_script|vhs_tape_stem
#
# Target project: SPUR_DEMO_PROJECT (default: this monorepo).
# Wait strings are soft (real projects differ); shell-use UAT uses the same
# anchors the tapes use.

lineage-dashboard|lineage-dashboard.sh|01-lineage-dashboard
sessions-picker|sessions-picker.sh|02-sessions-picker
palette-open|palette-open.sh|03-palette-open
