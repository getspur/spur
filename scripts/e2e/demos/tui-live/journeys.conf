# Live-project storyboard.
# Format: name|shell_use_script|vhs_tape_stem|flags
#
# flags:
#   (empty)      always run
#   agent-send   requires SPUR_DEMO_ALLOW_AGENT_SEND=1 (real model spend)
#
# Marketing / value demos = problem stories only (see PROBLEM_STORIES.md).
# Capture just those: SPUR_DEMO_STORIES_ONLY=1 ./render.sh
# Film pacing for shell-use: SPUR_DEMO_STORY_PACE=1 (capture-live-seed defaults on).
# Short surface probes remain for regression / component harvest — not marketing film.

# --- problem stories (value demos, ordered by marketing value) ---
# Full control plane: plan campaigns + lineage brain↔worker loop navigation.
problem-plan-loop-drive|problem-plan-loop-drive.sh|13-problem-plan-loop-drive|
product-e2e-flow|product-e2e-flow.sh|09-product-e2e-flow|
problem-ops-visibility|problem-ops-visibility.sh|10-problem-ops-visibility|
problem-plan-progress|problem-plan-progress.sh|11-problem-plan-progress|
problem-backlog-triage|problem-backlog-triage.sh|12-problem-backlog-triage|

# --- surface probes (regression only — demoted from marketing path) ---
lineage-dashboard|lineage-dashboard.sh|01-lineage-dashboard|
sessions-picker|sessions-picker.sh|02-sessions-picker|
palette-open|palette-open.sh|03-palette-open|
session-resume|session-resume.sh|04-session-resume|
explore-browser|explore-browser.sh|05-explore-browser|
explore-agents-tab|explore-agents-tab.sh|06-explore-agents-tab|
composer-draft|composer-draft.sh|07-composer-draft|

# --- opt-in agent turn (costs tokens; not default marketing) ---
agent-send|agent-send.sh|08-agent-send|agent-send
