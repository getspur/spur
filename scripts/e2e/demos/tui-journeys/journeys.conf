# Arc A — first-run / empty workspace (JOURNEYS.md story points).
# Format: name|fixture|shell_use_script|vhs_tape_stem
# shell_use_script is relative to scripts/e2e/shell-use/journeys/
# vhs_tape_stem is under tapes/ without .tape (empty = UAT only, no media)
#
# Dual purpose:
#   uat.sh --mode uat      → shell-use only (feature UAT)
#   uat.sh --mode capture  → VHS media only (demo assets)
#   uat.sh --mode all      → UAT then capture (default)

cold-launch|no-agents|cold-launch.sh|01-cold-launch
help-overlay|no-agents|help-overlay.sh|02-help-overlay
palette-open|no-agents|palette-open.sh|03-palette-open
explore-browser-open|no-agents|explore-browser-open.sh|04-explore-browser-open
clean-quit|no-agents|clean-quit.sh|05-clean-quit
